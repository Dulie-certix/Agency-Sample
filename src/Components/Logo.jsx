export default function Logo({ className = "" }) {
  return (
    <div className={`text-[#1090CB] 2xl:text-[31px] xl:text-[29px] lg:text-[24px] md:text-[22px] sm:text-[20px] text-[18px] font-bold ${className}`}>
      LOGO
    </div>
  );
}